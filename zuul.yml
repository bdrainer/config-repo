server:
  port: 8765
  
zuul:
  routes:
    patronservice: /patron/**
    paymentservice: /pay/**
    accountservice: /acct/**

endpoints:
  restart:
    enabled: true
  shutdown:
    enabled: true
  health:
    sensitive: false

logging:
  level:
    ROOT: INFO
    org.springframework.web: DEBUG